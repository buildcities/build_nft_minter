export const passportUrls = {
  genesis: "https:/build-genesis.netlify.app/",
  founders: "https:/build-founders.netlify.app/",
  regular: "https:/build-regular.netlify.app/",
} as Record<string, string>;
