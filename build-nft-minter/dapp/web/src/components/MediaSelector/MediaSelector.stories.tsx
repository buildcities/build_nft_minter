import MediaSelector from './MediaSelector'
import 'src/utils/firebase'
export const generated = () => {
  return <MediaSelector name="nice-name" />
}

export default { title: 'Components/MediaSelector' }
